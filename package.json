{
  "name": "typing-game",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare": "husky install",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "format": "npx prettier --write .",
    "build": "npm run build:frontend && npm run build:backend && npm run build:packages",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:packages": "cd packages/api-types && npm run build && cd ../question-sets && npm run build && cd ../score && npm run build && cd ../types && npm run build",
    "type-check": "npm run type-check:frontend && npm run type-check:backend && npm run type-check:packages",
    "type-check:frontend": "cd frontend && npm run type-check",
    "type-check:backend": "cd backend && npm run type-check",
    "type-check:packages": "cd packages/api-types && npm run type-check && cd ../question-sets && npm run type-check && cd ../score && npm run type-check && cd ../types && npm run type-check"
  },
  "license": "MIT",
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.7.3",
    "@typescript-eslint/parser": "^6.7.3",
    "eslint": "^8.50.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-config-standard-with-typescript": "^39.1.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.3",
    "husky": "^8.0.3",
    "lint-staged": "^14.0.1",
    "prettier": "^3.0.3"
  }
}
